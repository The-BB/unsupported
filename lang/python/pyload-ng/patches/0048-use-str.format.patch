From 1a5dd2c62a06f754bbc9522f16ca759c848e5683 Mon Sep 17 00:00:00 2001
From: GammaC0de <gammac0de@users.noreply.github.com>
Date: Sun, 16 May 2021 18:06:01 +0300
Subject: [PATCH 048/150] use str.format()

---
 src/pyload/plugins/anticaptchas/HCaptcha.py     | 2 +-
 src/pyload/plugins/downloaders/AlldebridCom.py  | 5 +++--
 src/pyload/plugins/downloaders/DebridlinkFr.py  | 4 ++--
 src/pyload/plugins/downloaders/PremiumTo.py     | 2 +-
 src/pyload/plugins/downloaders/RealdebridCom.py | 2 +-
 5 files changed, 8 insertions(+), 7 deletions(-)

--- a/src/pyload/plugins/anticaptchas/HCaptcha.py
+++ b/src/pyload/plugins/anticaptchas/HCaptcha.py
@@ -88,7 +88,7 @@ class HCaptcha(CaptchaService):
             m = re.search(self.KEY_FORMAT_PATTERN, key)
             if m is not None:
                 self.key = key
-                self.log_debug("Key: %s" % self.key)
+                self.log_debug("Key: {}".format(self.key))
                 return self.key
 
             else:
--- a/src/pyload/plugins/downloaders/AlldebridCom.py
+++ b/src/pyload/plugins/downloaders/AlldebridCom.py
@@ -31,8 +31,9 @@ class AlldebridCom(MultiDownloader):
     URL_REPLACEMENTS = [
         (
             r"https?://(?:www\.)?mega(?:\.co)?\.nz/#N!(?P<ID>[\w^_]+)!(?P<KEY>[\w\-,=]+)###n=(?P<OWNER>[\w^_]+)",
-            lambda m: "https://mega.nz/#!%s!%s~~%s"
-            % (m.group("ID"), m.group("KEY"), m.group("OWNER")),
+            lambda m: "https://mega.nz/#!{}!{}~~{}".format(
+                m.group("ID"), m.group("KEY"), m.group("OWNER")
+            ),
         ),
         (
             r"https?://(?:www\.)?mega(?:\.co)?\.nz/.*",
--- a/src/pyload/plugins/downloaders/DebridlinkFr.py
+++ b/src/pyload/plugins/downloaders/DebridlinkFr.py
@@ -24,7 +24,7 @@ def error_description(error_code):
         "maxTorrent": "Limitation of torrents per day reached",
     }.get(error_code)
 
-    return err_message or "Unknown error: '%s'" % error_code
+    return err_message or "Unknown error: '{}'".format(error_code)
 
 
 class DebridlinkFr(MultiDownloader):
@@ -56,7 +56,7 @@ class DebridlinkFr(MultiDownloader):
             self.req.http.c.setopt(
                 pycurl.HTTPHEADER, ["Authorization: Bearer " + api_token]
             )
-        self.req.http.c.setopt(pycurl.USERAGENT, "pyLoad/%s" % self.pyload.version)
+        self.req.http.c.setopt(pycurl.USERAGENT, "pyLoad/{}".format(self.pyload.version))
         try:
             json_data = self.load(self.API_URL + method, get=get, post=post)
         except BadHeader as exc:
--- a/src/pyload/plugins/downloaders/PremiumTo.py
+++ b/src/pyload/plugins/downloaders/PremiumTo.py
@@ -60,7 +60,7 @@ class PremiumTo(MultiDownloader):
 
             self.remove(self.last_download)
             self.fail(
-                self._("API error %s - %s") % (json_data["code"], json_data["message"])
+                self._("API error {} - {}").format(json_data["code"], json_data["message"])
             )
 
         return super().check_download()
--- a/src/pyload/plugins/downloaders/RealdebridCom.py
+++ b/src/pyload/plugins/downloaders/RealdebridCom.py
@@ -73,7 +73,7 @@ class RealdebridCom(MultiDownloader):
                 self.retry()
 
             else:
-                self.fail("%s (code: %s)" % (data["error"], data["error_code"]))
+                self.fail("{} (code: {})".format(data["error"], data["error_code"]))
 
         else:
             if data["filename"]:
